<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE policymap [
<!ELEMENT policymap (policy)*>
<!ATTLIST policymap xmlns CDATA #FIXED ''>
<!ELEMENT policy EMPTY>
<!ATTLIST policy xmlns CDATA #FIXED '' domain NMTOKEN #REQUIRED
  name NMTOKEN #IMPLIED pattern CDATA #IMPLIED rights NMTOKEN #IMPLIED
  stealth NMTOKEN #IMPLIED value CDATA #IMPLIED>
]>
<!--
  Hardened ImageMagick policy used by h5ai for thumbnail generation.
  Loaded via the MAGICK_CONFIGURE_PATH environment variable (see Setup::add_paths()),
  it applies to both the Imagick PHP extension and the `convert`/`gm` CLI tools.

  Goal: prevent SSRF / local file disclosure (the "ImageTragick" class of bugs)
  when ImageMagick processes user-controllable files. Coders that can reach the
  network or the filesystem indirectly are disabled.
-->
<policymap>
  <!-- Disallow ImageMagick from reaching out to the network or reading
       arbitrary local files through delegate/coder side channels. -->
  <policy domain="coder" rights="none" pattern="URL" />
  <policy domain="coder" rights="none" pattern="HTTPS" />
  <policy domain="coder" rights="none" pattern="HTTP" />
  <policy domain="coder" rights="none" pattern="FTP" />
  <policy domain="coder" rights="none" pattern="MSL" />
  <policy domain="coder" rights="none" pattern="MVG" />
  <policy domain="coder" rights="none" pattern="SVG" />
  <policy domain="coder" rights="none" pattern="TEXT" />
  <policy domain="coder" rights="none" pattern="LABEL" />
  <policy domain="coder" rights="none" pattern="EPHEMERAL" />
  <policy domain="coder" rights="none" pattern="PLT" />
  <policy domain="coder" rights="none" pattern="XPS" />
  <policy domain="coder" rights="none" pattern="PS" />
  <policy domain="coder" rights="none" pattern="PS2" />
  <policy domain="coder" rights="none" pattern="PS3" />
  <policy domain="coder" rights="none" pattern="EPS" />
  <policy domain="coder" rights="none" pattern="PDF" />
  <policy domain="coder" rights="none" pattern="XPS" />

  <!-- Block indirect reads via "@filename" / "@-" syntax. -->
  <policy domain="path" rights="none" pattern="@*" />

  <!-- Block delegates (external command execution) entirely. -->
  <policy domain="delegate" rights="none" pattern="*" />

  <!-- Reasonable resource limits to avoid decompression-bomb DoS. -->
  <policy domain="resource" name="memory" value="256MiB" />
  <policy domain="resource" name="map" value="512MiB" />
  <policy domain="resource" name="width" value="32KP" />
  <policy domain="resource" name="height" value="32KP" />
  <policy domain="resource" name="area" value="128MP" />
  <policy domain="resource" name="disk" value="1GiB" />
</policymap>
