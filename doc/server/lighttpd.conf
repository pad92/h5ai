# Adjust /_h5ai/ if h5ai uses another URL prefix.
$HTTP["url"] =~ "^/_h5ai/(?:private(?:/|$)|\\.htaccess$)" {
    url.access-deny = ("")
}

# Keep directory listing disabled for h5ai's internal directories.
$HTTP["url"] =~ "^/_h5ai/" {
    dir-listing.activate = "disable"
}
